<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :title %>
    <%= form.text_field :title, class: "form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :published_year %>
    <%= form.number_field :published_year, class: "form-control" %>
  </div>

  <div class="field form-group">
    <% select_tag "Author", options_from_collection_for_select(Author.all, :id, :name), {:class=>'form-control'}  %>
    <%= form.select :author_id, options_from_collection_for_select(Author.all, :id, :name), {:class=>'form-control'}%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
